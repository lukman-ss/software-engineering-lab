# Open Questions

- What is the most effective approach for linting cross-references (e.g., verifying that a `Superseded by ADR-XXX` tag refers to an existing, valid ADR file)?
- Should ADR indices (e.g., `docs/adr/README.md`) be maintained manually or generated automatically via CI/CD pre-commit hooks?
- How should teams handle cross-repository architectural decisions in multi-repo microservice setups (e.g., central architecture repo vs git submodules)?
